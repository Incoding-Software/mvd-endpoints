
import org.json.JSONException;
import org.json.JSONObject;	


     
public class GetCustomerResponse {
        public String Title;
        public int Number;
        public MyClass Custom;
            
         public static GetCustomerResponse[] Create(JSONObject result) throws JSONException, ModelStateException  {     
		     IncodingHelper.Verify(result);
			 if(result.isNull("data"))
               return new  GetCustomerResponse[0];

		  
		     JSONObject data = new JSONObject(result.getString("data"));    
	         GetCustomerResponse res = new GetCustomerResponse();
                          res.Title = data.getString("Title");
                          res.Number = data.getInt("Number");
                          res.Custom = data.get("Custom");
              
		     return res;   
	      
         }   

 	                                                           
}